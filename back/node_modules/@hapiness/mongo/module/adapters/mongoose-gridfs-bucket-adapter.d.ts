import * as mongo from 'mongodb';
import { MongooseAdapter } from './mongoose-adapter';
export declare class MongooseGridFsBucketAdapter extends MongooseAdapter {
    private _gridfsBucket;
    static getInterfaceName(): string;
    constructor(options: any);
    getLibrary<T = mongo.GridFSBucket>(): T;
}
