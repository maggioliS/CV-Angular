/// <reference types="mongoose" />
import { Connection, Mongoose } from 'mongoose';
import { Observable } from 'rxjs/Observable';
import { HapinessMongoAdapter } from './hapiness-mongo-adapter';
export declare class MongooseAdapter extends HapinessMongoAdapter {
    static getInterfaceName(): string;
    constructor(options: any);
    protected _tryConnect(): Observable<void>;
    protected _afterConnect(): Observable<void>;
    getLibrary<T = Mongoose>(): T;
    getConnection<T = Connection>(): T;
    registerValue(schema: any, collection: string, collectionName?: string): any;
    close(): Observable<void>;
}
