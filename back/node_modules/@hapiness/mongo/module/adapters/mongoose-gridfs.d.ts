/// <reference types="gridfs-stream" />
import { GridFsStream } from '../shared/gridfs-stream';
import { MongooseAdapter } from './mongoose-adapter';
/**
 * Gridfs adapter using mongoose for connection purposes
 *
 * @deprecated
 * @export
 * @class MongooseGridFsAdapter
 * @extends {HapinessMongoAdapter}
 */
export declare class MongooseGridFsAdapter extends MongooseAdapter {
    private _gridfs;
    protected _client: any;
    static getInterfaceName(): string;
    constructor(options: any);
    protected _createGridFsStream(db: any, mongo: any): GridFsStream.Grid;
    getLibrary<T = GridFsStream.Grid>(): T;
}
